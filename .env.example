# Database PostgreSQL — Usa Neon (https://neon.tech) per un DB gratuito
DATABASE_URL="postgresql://user:password@host:5432/gestionale_paninoteca?sslmode=require"

# JWT per login
JWT_SECRET="una-stringa-molto-sicura-e-lunga"

# Chiave per lo script di scraping incassi (POST /api/incassi senza cookie)
INCASSI_API_KEY=""

# Cron Velocissimo (tutto su Vercel): imposta le stesse variabili su Vercel
# VELOCISSIMO_EMAIL=magazzino@daddysburger.it
# VELOCISSIMO_PASSWORD=
# VELOCISSIMO_SEDE=Sorrento   (testo da cliccare in pagina dopo il login; nei passaggi vedi "cosa vedo" e scegli tu)
# VELOCISSIMO_PUNTO_VENDITA_ID=   (opzionale, altrimenti usa primo punto vendita attivo)
# CRON_SECRET=   (opzionale, per proteggere /api/cron/sync-velocissimo)
